(function(){
  function trailsort () {
    return function(itemSorting) {
      if itemSorting = difficulty {

      } else if itemSorting = time {

      } else if itemSorting = length {
        
      }
    };
  }

  angular
    .module('sloHiker')
    .filter('trailsort', trailsort);
})();
